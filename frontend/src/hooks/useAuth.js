export default function useAuth() {
    const is_authenticated = false;
    const login = (email, password) => {};
    const register = (email, username, password) => {};

    return {is_authenticated, login, register};
}
